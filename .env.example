# Apple Watch Approval - configuration
# Copy this file to .env and edit as needed.

# ntfy.sh topic name (subscribe to this in the ntfy app)
# Use a unique name to avoid collisions with other users.
NTFY_TOPIC=claude-approval-xxxxxxxx

# ntfy.sh server URL (change for self-hosted instances)
NTFY_SERVER=https://ntfy.sh

# Seconds to wait before auto-denying if no response
APPROVAL_TIMEOUT=60

# Local server port
PORT=8765

# Public URL for action buttons (Approve / Deny).
# Set automatically by start.sh via Cloudflare Tunnel.
# Uncomment to override manually:
# PUBLIC_URL=https://your-tunnel.trycloudflare.com
